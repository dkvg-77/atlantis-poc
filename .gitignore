# terraform
.terraform/
*.tfstate
*.tfstate.*
.terraform.lock.hcl
crash.log
# the demo output the local_file resource would write
terraform/demo-output.txt

# Claude Code session artifacts (never commit)
.claude/
